# QA

## Q: 这次启动失败是项目代码本身有问题吗，还是环境依赖问题，最后怎么处理的？
A: 这次主要是环境和依赖问题，不是项目核心代码 bug。根因是 `overlay-ui` 依赖不完整导致 `tauri` 命令不可用、UI 子进程启动失败；处理方式是在 `overlay-ui` 目录重装依赖并恢复 `tauri-cli`，之后项目可正常启动（`1420` 与 `19823` 端口正常监听）。另有一个独立配置风险：`config/config.json` 中的 `aude-opus` 很可能是模型名拼写错误，建议改为有效的 `claude-opus-...` 模型名。
