# Message Merge Prompt

你需要判断新消息是否可以与现有任务合并。

## 现有任务列表

{{todos}}

## 新消息

{{newMessage}}

---

请分析新消息与现有任务的关系：

1. 如果新消息是对现有任务的补充或修改，调用 `merge_task` 工具
2. 如果新消息是全新的独立任务，调用 `create_task` 工具
3. 如果新消息是取消或暂停某个任务，调用 `cancel_task` 工具

判断依据：
- 是否涉及相同的应用或操作对象
- 是否是对之前任务的澄清或补充
- 时间上是否紧密相关
